# Streamfold X adapter

归页内置的 X 平台适配器。它作为可信签名平台能力随应用默认启用，也可由用户随时关闭；适配器通过受限浏览器会话被动读取 X 已登录网页自身产生的 JSON 响应，用于同步本人公开资料和本人原创或引用帖子。

## 数据范围

- 身份核验始终先等待 `account/settings.json` 的首个声明响应并读取登录 handle，再用 `UserByScreenName` 返回当前实际登录账号和稳定账号 ID；确认与同步前后都重新执行同一条会话绑定链，并由宿主核对预期稳定 ID。
- 未绑定账号打开 X 官方页面后，清单驱动的后台 supervisor 会持续监听身份响应；监听通道中断时按 2 秒到 60 秒指数退避自动重连，识别到身份后提示用户确认绑定。
- 支持仅资料、最近 20 条和最近 100 条三种范围。
- `recent_20` 最多监听 5 个时间线响应，`recent_100` 最多监听 20 个；若有界监听达到响应数或总量限制后仍有 Bottom cursor，会返回已验证的本人内容，并附带实际响应数、上限和内容数警告。
- 支持配置手动采集间隔，并按账号或分组创建自动运行计划。
- 排除回复、纯转帖及其他账号内容；帖子链接统一为 `https://x.com/i/web/status/{id}`。
- 映射浏览、点赞、回复、转帖、书签和引用数；不把账号主动点赞数当作收到的累计点赞。

## 安全边界

插件不使用 X 开发者 API、API Key、Cookie 导入、请求头读取、DOM 解析或自动验证码。后台缓存只接收清单白名单中的公开身份标量，且 `UserByScreenName` 必须由响应中的 handle 与受控页面 handle 关联后才会进入缓存；未声明字段和其他账号的同 operation 响应不会进入插件沙箱。登录和登录挑战必须由用户在 X 页面中手动完成。重复游标、冲突重复、响应结构错误、X 内部网页接口变化、限流或挑战仍会使同步失败。

Copyright 2026 Streamfold Contributors. Licensed under Apache-2.0.
